return {
	opts = {
		-- 4 wide spaces
		shiftwidth = 4,
		tabstop = 4,
		expandtab = true,

        -- line numbers
        number = true,
        relativenumber = true,

        -- always show side column
        signcolumn = "yes",

        timeoutlen = 150,
    },
    gs = {
        mapleader = " ",
    }
}
